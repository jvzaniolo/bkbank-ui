import dedent from 'ts-dedent';
import { Canvas, Description, Meta, Story } from '@storybook/blocks';
import * as InputStories from './Input.stories';

<Meta of={InputStories} />

# Input

<Description
  markdown={dedent`
  | Selector | Type | Description |
  | --- | --- | --- |
  | \`.input\` | Component | The input component |
  | \`.input-label\` | Component | The input label component |
  | \`.input-md\` | Modifier | Changes the size to medium |
  | \`.input-lg\` | Modifier | Changes the size to large |
  | \`:invalid\`\\|\`.input-invalid\` | Modifier | Changes to state to invalid |
  | \`:disabled\` | Modifier | Changes to state to disabled |
`}
/>

## Default

<Canvas of={InputStories.Default} />

## Sizes

<Canvas of={InputStories.Sizes} />

## With Label

<Canvas of={InputStories.WithLabel} />

## Floating Label

**Important:** Make sure to follow the example HTML structure to ensure proper styling and behavior. More specifically, the `.input-label` must come immediately after the `input` and the `placeholder` content must be set to a single space `" "`.

<Canvas of={InputStories.FloatingLabel} />

## Invalid

<Canvas of={InputStories.Invalid} />

## Disabled

<Canvas of={InputStories.Disabled} />
