import dedent from 'ts-dedent';
import { Canvas, Description, Meta, Story } from '@storybook/blocks';
import * as SwitchStories from './Switch.stories';

<Meta of={SwitchStories} />

# Switch

<Description
  markdown={dedent`
    | Selector | Type | Description |
    | --- | --- | --- |
    | \`.switch[type="checkbox"]\` | Component | The switch component |
    | \`.switch-label\` | Component | The label for the switch component |
    | \`.switch-md\` | Modifier | Changes the size to medium |
    | \`:invalid\`\\|\`.switch-invalid\` | Modifier | Changes to state to invalid |
    | \`:disabled\` | Modifier | Changes to state to disabled |
  `}
/>

## Default

<Canvas of={SwitchStories.Default} />

## Sizes

<Canvas of={SwitchStories.Sizes} />

## With label

Targets the immediate `.switch-label` element after the `input.switch` to apply the label styling.

<Canvas of={SwitchStories.WithLabel} />

## Invalid

<Canvas of={SwitchStories.Invalid} />

## Disabled

<Canvas of={SwitchStories.Disabled} />
