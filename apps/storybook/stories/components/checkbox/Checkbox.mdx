import dedent from 'ts-dedent';
import { Canvas, Description, Meta, Story } from '@storybook/blocks';
import * as CheckboxStories from './Checkbox.stories';

<Meta of={CheckboxStories} />

# Checkbox

<Description
  markdown={dedent`
    | Selector | Type | Description |
    | --- | --- | --- |
    | \`.checkbox\` | Component | The checkbox component |
    | \`.checkbox-label\` | Component | The checkbox label component |
    | \`:invalid\`\\|\`.checkbox-invalid\` | Modifier | Changes to state to invalid |
    | \`:disabled\` | Modifier | Changes to state to disabled |
  `}
/>

## Default

<Canvas of={CheckboxStories.Default} />

## With Label

Targets the immediate `.checkbox-label` element after the `input.checkbox` to apply the label styling.

<Canvas of={CheckboxStories.WithLabel} />

## Invalid

Targets the `:invalid` attribute or the `.checkbox-invalid` class to apply the stylings.

<Canvas of={CheckboxStories.Invalid} />

## Disabled

Targets the `:disabled` attribute to apply the stylings.

<Canvas of={CheckboxStories.Disabled} />
