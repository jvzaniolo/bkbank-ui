import dedent from 'ts-dedent';
import { Canvas, Description, Meta, Story } from '@storybook/blocks';
import * as BreadcrumbStories from './Breadcrumb.stories';

<Meta of={BreadcrumbStories} />

# Breadcrumb

Breadcrumb is a navigation component that shows the current page's location within a navigational hierarchy.

See the [W3C documentation](https://www.w3.org/WAI/ARIA/apg/patterns/breadcrumb/examples/breadcrumb/) for information on accessibility.

<Description
  markdown={dedent`
    | Selector | Type | Description |
    | --- | --- | --- |
    | \`.breadcrumb\` | Component | Base class |
    | \`a\` | Component | The breadcrumb item |
    | \`[aria-current='page']\` | Component | The item that represents the current page |
  `}
/>

## Default

<Canvas of={BreadcrumbStories.Default} />
