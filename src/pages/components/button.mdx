import { Button } from '~/lib'
import { UserIcon } from '@heroicons/react/24/outline'

# Button

The Button component is used to trigger an action or event, such as submitting a form, opening a dialog, canceling an action, or performing a delete operation.

<div className="docs-container">
  <Button>Button</Button>
</div>

```jsx copy
<Button>Button</Button>
```

## Size

<div className="docs-container">
  <Button size="sm">Small</Button>
  <Button size="md">Medium</Button>
  <Button size="lg">Large</Button>
</div>

```jsx copy
<Button size="sm">Small</Button>
<Button size="md">Medium</Button>
<Button size="lg">Large</Button>
```

## Variant

<div className="docs-container">
  <Button variant="solid">Solid</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="ghost">Ghost</Button>
</div>

```jsx copy
<Button variant="solid">Solid</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>
```

## Color

<div className="docs-container">
  <Button color="primary">Primary</Button>
  <Button color="secondary">Secondary</Button>
</div>

```jsx copy
<Button color="primary">Primary</Button>
<Button color="secondary">Secondary</Button>
```

## Icon

<div className="docs-container">
  <Button>
    <UserIcon />
    <span>With left icon</span>
  </Button>
  <Button>
    <span>With right icon</span>
    <UserIcon />
  </Button>
</div>

```jsx copy
// import { UserIcon } from '@heroicons/react/24/outline'

<Button><UserIcon /> With left icon</Button>
<Button>With right icon <UserIcon /></Button>
```

## Loading

<div className="docs-container">
  <Button isLoading>Button</Button>
</div>

```jsx copy
<Button isLoading>Button</Button>
```

## Disabled

<div className="docs-container">
  <Button disabled>Button</Button>
  <Button variant="outline" disabled>
    <span>Button</span>
  </Button>
  <Button variant="ghost" disabled>
    <span>Button</span>
  </Button>
</div>

```jsx copy
<Button disabled>Button</Button>
<Button variant="outline" disabled>Button</Button>
<Button variant="ghost" disabled>Button</Button>
```

## API

The Button component accepts the following props:

### `asChild` (optional)

Whether the button is a child of another component or not.

- Type: `boolean`

### `size` (optional)

The size of the button.

- Type: `'sm' | 'md' | 'lg'`
- Default: `'sm'`

### `variant` (optional)

The variant of the button.

- Type: `'solid' | 'outline' | 'ghost'`
- Default: `'solid'`

### `color` (optional)

The color of the button.

- Type: `'primary' | 'secondary'`
- Default: `'primary'`

### `isLoading` (optional)

Whether the button is loading or not.

- Type: `boolean`

### `disabled` (optional)

Disables the button.

- Type: `boolean`
