import dedent from 'ts-dedent';
import { Canvas, Description, Meta, Story } from '@storybook/blocks';
import * as RadioStories from './Radio.stories';

<Meta of={RadioStories} />

# Radio

<Description
  markdown={dedent`
    | Selector | Type | Description |
    | --- | --- | --- |
    | \`.radio\` | Component | The radio component |
    | \`.radio-label\` | Component | The label for the radio component |
    | \`:invalid\`\\|\`.radio-invalid\` | Modifier | Changes to state to invalid |
    | \`:disabled\` | Modifier | Changes to state to disabled |
  `}
/>

## Default

<Canvas of={RadioStories.Default} />

## With label

Targets the immediate `.radio-label` element after the `input.radio` to apply the label styling.

<Canvas of={RadioStories.WithLabel} />

## Invalid

Targets the `:invalid` attribute or the `.radio-invalid` class to apply the stylings.

<Canvas of={RadioStories.Invalid} />

## Disabled

Targets the `:disabled` attribute to apply the stylings.

<Canvas of={RadioStories.Disabled} />
